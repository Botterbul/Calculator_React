export default function calculate(object, btnName) {
    if (btnName == "clear") {
        return {
            newValue: null,
            totalValue: null,
            operator: null,
        }
    }
}
